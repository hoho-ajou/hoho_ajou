# Git 커밋/푸시 정책

## 커밋

- **커밋 전 항상 사용자에게 먼저 물어본다.** 사용자가 명시적으로 요청하기 전까지 어떤 커밋도 만들지 않는다.
- 커밋 메시지 제목/본문에 "AI", "Claude" 등 AI 관련 언급을 넣지 않는다.
- 커밋 메시지 마지막 줄에 `Co-Authored-By: Claude ...`가 Claude Code 시스템 정책으로 자동으로 붙는다. 이 줄은 커밋을 만드는 시점에는 제거할 수 없음.
  - 이 줄이 없는 커밋 이력이 필요하면, 커밋 생성 후 사용자가 직접(또는 요청 시) `git commit --amend`나 `git rebase -i`로 메시지를 다시 써서 제거한다. 이는 기존 커밋을 고치는 작업이라 가능함.
  - 실제 GitHub 저장소로 옮길 때는 옮기기 직전에 전체 커밋을 정리(rebase/squash)해서 Co-Authored-By 없는 상태로 올리는 것을 기본으로 한다.

## 푸시

- **푸시 전 항상 사용자에게 먼저 물어본다.** origin에 올라가는 순간 팀원 전체(또는 외부)에게 보이는 상태 변경이므로, 사전 승인 없이 절대 실행하지 않는다.
- force push는 사용자가 명시적으로 요청한 경우에만, 그리고 영향 범위(브랜치, 덮어써질 커밋)를 먼저 설명한 뒤 실행한다.

## 적용 범위

이 정책은 `/home/user/wemeet` 저장소의 모든 git 작업에 적용된다. `CLAUDE.md`에서 이 파일을 참조한다.
